# Entorno virtual
env/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Django
db.sqlite3
db.sqlite3-journal
*.log
/staticfiles/
/media/

# Datos reales (volcados de migración, no son fixtures de test para versionar):
# contienen horarios, tareas y notas personales del autor.
# Anclado a la raiz: postman/fixtures/ SI se versiona (CSV de ejemplo de 5 filas).
/fixtures/

# Esquema OpenAPI generado; se regenera con `manage.py spectacular`.
schema.yml

# Secretos / config local
.env
.env.*.local
# env de docker compose (la plantilla .env.docker.example sí se versiona)
.env.docker

# IaC — estado y secretos de Terraform (los .tf y *.example SÍ se versionan)
infra/terraform/.terraform/
infra/terraform/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Cobertura de tests
.coverage
.coverage.*
coverage.xml
htmlcov/

# Test / IDE
.pytest_cache/
.vscode/
.idea/

# SO
Thumbs.db
Desktop.ini
.DS_Store

# Guía de desarrollo con IA (uso local, no se publica en el repo)
CLAUDE.md

# Bitácora personal del proyecto (uso local)
memory.md

# Registro de trabajo pendiente (uso local)
pendientes.md

# Aprendizaje tecnico por tecnologia (uso local)
aprendizaje.md

# Config local de Claude Code (personal, no versionar)
.claude/settings.local.json

# Capturas y trazas de Playwright MCP (verificacion visual, no versionadas)
.playwright-mcp/

# Storage persistente de NiceGUI (tema elegido)
.nicegui/
