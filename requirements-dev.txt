# Dependencias de desarrollo / testing.
-r requirements.txt
# Deps de la UI (streamlit, plotly, etc.): los tests importan gantt.py y las vistas,
# que usan plotly — sin esto la RECOLECCIÓN de pytest falla (exit 2), como pasó en CI.
-r streamlit_ui/requirements.txt
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0          # integra coverage.py con pytest (pytest --cov)
coverage>=7.0            # coverage.py (motor de cobertura; usable también standalone)
responses>=0.25          # mock de HTTP para los tests de la UI Streamlit
# unittest.mock es de la stdlib (no requiere instalación).
