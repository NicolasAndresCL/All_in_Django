# Dependencias de desarrollo / testing.
-r requirements.txt
# Deps de la UI (nicegui, plotly, etc.): los tests importan gantt.py y las vistas,
# que usan plotly/nicegui — sin esto la RECOLECCIÓN de pytest falla (exit 2).
-r nicegui_ui/requirements.txt
pytest>=8.0
pytest-asyncio>=0.24     # tests async de la UI NiceGUI (nicegui.testing User)
pytest-django>=4.8
pytest-cov>=5.0          # integra coverage.py con pytest (pytest --cov)
coverage>=7.0            # coverage.py (motor de cobertura; usable también standalone)
responses>=0.25          # mock de HTTP para los tests de la UI
# unittest.mock es de la stdlib (no requiere instalación).
