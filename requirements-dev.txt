# Dependencias de desarrollo / testing.
-r requirements.txt
# Deps de la UI (nicegui, plotly, etc.): los tests importan gantt.py y las vistas,
# que usan plotly/nicegui — sin esto la RECOLECCIÓN de pytest falla (exit 2).
-r nicegui_ui/requirements.txt
pytest>=9.1.1
pytest-asyncio>=1.4.0     # tests async de la UI NiceGUI (nicegui.testing User)
pytest-django>=4.14.0
pytest-cov>=7.1.0          # integra coverage.py con pytest (pytest --cov)
coverage>=7.16.0            # coverage.py (motor de cobertura; usable también standalone)
responses>=0.26.3          # mock de HTTP para los tests de la UI
ruff>=0.16.5               # linter del job `lint` del CI (config en pyproject.toml)
# Verificacion visual en navegador real (marca `visual`, excluida por defecto en
# pytest.ini y fuera del CI). Mide computed style y contraste; requiere descargar el
# navegador una vez:  python -m playwright install chromium
playwright>=1.48
# unittest.mock es de la stdlib (no requiere instalación).
